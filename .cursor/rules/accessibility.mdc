---
description: Accessibility patterns (WCAG 2.2 / WAI-ARIA APG) for UI code
globs: ["**/*.html", "**/*.css", "**/*.scss", "**/*.jsx", "**/*.tsx", "**/*.vue", "**/*.svelte", "**/*.astro"]
alwaysApply: false
---

# Accessibility

This project uses the [a11y-skills](https://github.com/Elizabeth1979/a11y-skills) pattern library.

**Route before you read.** Open `patterns/INDEX.md` — it maps a UI element or an audit symptom to
exactly one pattern file — then read only that file. The 43 pattern files are 8–16 KB each;
loading them all wastes context.

**Non-negotiable defaults.** Eleven rules that hold for every UI change live in the
"The Non-Negotiables" section of `patterns/accessibility.instructions.md` — read it once per
task. The shortest version: **native HTML before ARIA**, because an ARIA role is a promise to
implement its whole keyboard model.

**Scope.** Retrofitting an existing page changes markup and ARIA only — no restyling. The
exceptions are colour contrast and reduced motion, which are inherently visual; fix those in CSS
and say so.

**Verify.** Run `docs/verification.md`, then report which criteria still need a human pass.
